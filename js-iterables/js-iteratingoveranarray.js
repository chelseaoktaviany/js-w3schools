// you can use a for..of loop to iterate over the elements of an array:

// create an array
const letters = ["a", "b", "c"];

// list all elements
let text ="";
for (const x of letters) {
    text += x + "\n";
}

console.log(text);
/**
 * a
 * b
 * c
 */