/**
 * Math.random() - returns a random number between 0 (inclusive), and 1 (exclusive)
 */

//examples
console.log(Math.random());

//examples rounded
console.log(Math.round(Math.random() * 100) / 100);  //0.xx or 1.xx