/**
 * Math.cos() - returns the cosine (a value between -1 and 1) of the angle x (given in radians)
 */

//example
let res = Math.cos(0 * Math.PI / 180);
console.log("The cosine value of 0 degree is ", 1); 