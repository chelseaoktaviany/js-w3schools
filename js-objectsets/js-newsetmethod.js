// pass an array to the new set() constructor

// create a set
// const letters = new Set(["a", "b", "c"]);

// console.log(letters.size); // 3

// create a set and add literal values: (alternatively)
const letters = new Set();

// add values to the set
letters.add("a");
letters.add("b");
letters.add("c");

// display set size
console.log(letters.size); // 3