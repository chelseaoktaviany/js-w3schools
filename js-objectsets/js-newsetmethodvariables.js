// create a set
const letters = new Set();

// create a set and add a variables
const a = "a";
const b = "b";
const c = "c";

// add variables to the set
letters.add(a);
letters.add(b);
letters.add(c);

console.log("The size of letters sets is " + letters.size); // the size of letters sets is 3